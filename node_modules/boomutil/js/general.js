export const el = query => document.querySelector(query);
export const els = query => document.querySelectorAll(query);

export const go = url => window.open(url, "_blank");

export const cookies = {
	set: (key, value) => localStorage(key, value),
	get: key => localStorage.getItem(key)
}

export const reveal = () => {
	els(".reveal").forEach((view, i) => {
		if(view.getBoundingClientRect().top < window.innerHeight - 75) {
			view.classList.add("revealed");
		}
	});
}