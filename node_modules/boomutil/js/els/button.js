export default class CardButton extends HTMLElement {
	connectedCallback () {
		this.innerHTML = `<div>
			<div class="about">
				<h2>${this.getAttribute("title")}</h2>
				<p>${this.getAttribute("description")}</p>
			</div>
			<span>&#62;</span>
		</div>`
	}
}