export default class CardLayers extends HTMLElement {
	connectedCallback () {
		this.innerHTML = `<div>
			<img class="banner" 
				src="${this.getAttribute("banner")}"/>
			<div class="shadow"></div>
			<div class="about">${this.innerHTML}</div>
		</div>`
	}
}