export default class BoomSelect extends HTMLElement {
	set load(array) {
		let html = "";
		for(const tag of array) {
			html += `<boom-item title="${tag.title}" `;
			if(tag.icon) html += `icon="${tag.icon}"`;
			html += `></boom-item>`;
		}
		this.innerHTML = html;
	}
}


