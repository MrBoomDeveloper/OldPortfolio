export default class BoomButton extends HTMLElement {
	connectedCallback() {
		if(this.classList.contains("large")) {
			this.innerHTML = `
				<div class="info">
					<h2>${this.getAttribute("title")}</h2>
					<p>${this.getAttribute("description")}</p>
				</div>
				<span></span>
			`;
		}
	}
}